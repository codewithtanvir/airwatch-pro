# AirWatch Backend - Compatible Dependencies
# Updated for Python 3.13 and compatibility with ML service

# === CORE FRAMEWORK ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.11.0
pydantic-settings>=2.1.0

# === HTTP & API CLIENTS ===
httpx>=0.25.0
requests>=2.31.0
# aiohttp already installed with compatible version

# === DATABASE & SUPABASE ===
supabase>=2.3.0
postgrest>=0.16.0

# === DATA PROCESSING ===
# pandas and numpy already installed with newer versions
# scipy already installed
pytz>=2023.3
python-dateutil>=2.8.2

# === GEOSPATIAL & MAPPING ===
geopy>=2.4.0

# === ENVIRONMENTAL VARIABLES ===
python-dotenv>=1.0.0

# === ASYNC SUPPORT ===
aiofiles>=23.2.0

# === AUTHENTICATION & SECURITY ===
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
cryptography>=41.0.0

# === CACHING ===
# redis already installed with newer version
cachetools>=5.3.0

# === UTILITIES ===
click>=8.1.0
rich>=13.7.0

# === EMAIL ===
jinja2>=3.1.0

# === CONFIGURATION ===
pyyaml>=6.0.0

# === PRODUCTION DEPLOYMENT ===
gunicorn>=21.2.0